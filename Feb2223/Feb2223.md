# Feb22-23



## GAC Code卸载
```powershell
npm uninstall -g @anthropic-ai/claude-code
npm uninstall -g https://gaccode.com/claudecode/install
```
pnpm装Official Claude Code，npm装GAC版，一般终端输入`claude`默认启动pnpm版。想要运行npm版：`& "C:\Users\K\AppData\Roaming\npm\claude"`
另一方法，在GAC创建API，"C:\Users\K\.claude\settings.json"改为
```json
{
"env": {
"ANTHROPIC_API_KEY": "Your-Key",
"ANTHROPIC_BASE_URL": "https://gaccode.com/claudecode",
"CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1"
}
}
```
Git Bash路径
```bash
cd /c/A/B/C
cd "C:\A\B\C"
cd "$(cygpath 'C:\A\B\C')"
```


## GitHub操作

### 重命名

```bash
git clone <repo>
cd <repo>
git mv path/old.file path/new.file
git commit -m "Rename"
git push
```

（git commit -m "Rename"最后的字符串随意，但是不能为空）

### 要有提交者身份（邮箱最好和 GitHub 绑定，用户名随意）：

#### 方案 A：只对这个仓库生效（推荐，最不惹事）

在仓库目录里跑：

```bash
git config user.name "YourName"
git config user.email "Your@Email.com"
```

然后再提交：

```bash
git commit -m "Rename"
```

#### 方案 B：对你这台机器所有仓库生效

```bash
git config --global user.name "YourName"
git config --global user.email "Your@Email.com"
```

---

#### 检查一下有没有设成功

```bash
git config --list --show-origin | grep user
```

或更直观一点：

```bash
git config user.name
git config user.email
```

### 文件大小

- **≤ 25 MiB**：网页端可以 `Add file → Upload files` 上传。

- **25 MiB ~ 100 MiB**：网页端不让传；但你可以用 Git 推上去。超过 **50 MiB** 时 Git 会给警告（还能推，但不推荐长期这么干）。

- **> 100 MiB**：GitHub 会直接禁止你把这种大文件放进普通 Git 历史里；需要改用 **Git LFS**（大文件存储）或别的分发方式。可放在 **GitHub Releases 的附件**里：单个文件必须 **< 2 GiB**。

### 其它

- Fork 时 不需要完整的所有分支，就 `Copy the main branch only`



## ChatGPT风格

### 用伪代码把它钉死（类比版）

```pseudocode
function respond(user_input, personality):
    sys = SAFETY_SYSTEM_RULES   # 更高优先级边界（不随 personality 改）
    dev = APP_RULES             # 更高优先级业务规则（不随 personality 改）
    style = personality_config(personality)  # 只管“怎么说”

    if safety_gateway_blocks(user_input):    # 外部网关/分类器
        return refusal_in(style)

    raw = model.generate(sys + dev + user_input)  # 核心能力在这
    safe = enforce_policies(raw)                  # 可能还有后处理/再审
    return render_with_style(safe, style)         # 最后才是语气/排版
```

你调 Nerdy，本质上主要动的是 `render_with_style(...)` 这一段；而安全边界在 `SAFETY_SYSTEM_RULES / safety_gateway / enforce_policies`，优先级更高、位置更靠前。

宇宙级总结（这次不加彩蛋版本）：**人格是“皮肤”，安全是“权限系统”，能力是“引擎”。换皮肤不会突然给你 root 权限，也不会把引擎升级成另一台机器。**



## WSL

### 卸载重装

```powershell
wsl -l -v # 会列出你装的 Linux 发行版（Ubuntu / Debian 之类）和 WSL 版本（1/2）。
wsl --unregister <Name> # 这不会动 Windows 的 WSL 组件，只是把某个 Ubuntu 之类删掉再装回来。
wsl --unregister Ubuntu # 以 Ubuntu 为例
wsl --install -d Ubuntu
```

### 彻底重装

```powershell
dism /online /disable-feature /featurename:Microsoft-Windows-Subsystem-Linux /norestart
dism /online /disable-feature /featurename:VirtualMachinePlatform /norestart
```

```powershell
dism /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
```

### 可能修复成功

```powershell
wsl --shutdown
wsl --update
```

### CRLF or  LF

#### 鉴定

* 看类型：

  ```bash
  file your.txt
  ```

  如果看到 `CRLF` 字样，说明是 Windows 换行。

* 直接找回车符 `\r`：

  ```bash
  cat -A your.txt | head
  ```

  看到 `^M` 的话通常就是 CRLF。

#### 转换

* CRLF → LF：

  ```bash
  dos2unix your.txt
  ```

* LF → CRLF：

  ```bash
  unix2dos your.txt
  ```

- 把 Windows 文本文件“拖进” WSL2 **不会自动改换行符**；换行符变化通常发生在“编辑/保存、格式化、或 git 的换行策略”这些环节。

### 一次性创建路径 (-p)

```bash
mkdir -p /tmp/a/b # 会自动先建 /tmp/a，再建 /tmp/a/b
```


### 让WSL走和Windows一样的代理：Windows配置/新建文件 `~\.wslconfig`

```
[wsl2]
autoProxy=true
networkingMode=mirrored
dnsTunneling=true
```

- 还有一种走proxy的方式：将一个`.sh`文件（我的命名为`proxywsl.sh`，上传在GitHub）

```bash
sudo cp proxywsl.sh /etc/profile.d/ # 假设 proxywsl.sh 在当前目录
sudo chmod 644 /etc/profile.d/proxywsl.sh # 确保权限正确（可读可执行）

echo $http_proxy # 重新打开Terminal后确认
```

- WSL 运行ssh没反应，经debug发现是因为Clash的TUN Mode。有了这个配置以后可以不开TUN。

- 还有一种方法：Clash 开启 Allow LAN，WSL配置文件写入 

- 安装C语言编译器： `sudo apt install -y build-essential gdb`


### npm & pnpm 相关
```bash
# Install Node.js in WSL (recommended: nvm)
sudo apt update && sudo apt upgrade -y
sudo apt-get install -y curl
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/master/install.sh | bash # install nvm
exec $SHELL # reload your shell (or close/reopen terminal)
nvm install --lts # install Node (LTS)
node --version
npm --version

# 启用pnpm
corepack enable
corepack prepare pnpm@latest --activate
pnpm setup

# 看全局装的包
npm ls -g --depth=0
pnpm ls -g --depth=0
```



## 其它

- Google Drive 文件转移到另一个账号：`Share`→指定另一个号为`Editor`→`Transfer Ownership`→另一个号接到Gmail 点`Respond`→全选`Ctrl+A`→ `Share`→`Accept Transfer`

- Python没必要下最新版。现在3.12在Security阶段，3.13、3.14在bugfix阶段，3.15pre-release。想Python正常运行，3.12即可。


- CLI和IDE可以统称为AI coding agents/tools: `Write prompts for AI coding agents/tools, such as Cursor and Antigravity, or CLI tools like OpenAI Codex and Claude Code.`

- MATLAB R2024b 无官方暗色模式。只能在`Home->Preferences->Colors`自己调

- VS Code的Copilot不支持自定义Base URL i.e. `Bring Your Own Key (BYOK) ` AI推荐VS插件Continue

- To turn on **Dark Mode in Notepad++**: `Settings` `Preferences` `Dark Mode`

- Clash Verge Rev老版本bug多：建议回退版本2.3.1


- Football
  - `La Albiceleste` 白色+天蓝色 阿根廷队🇦🇷	
  - `La Hinchada Albiceleste` 🇦🇷球迷
  - `Los Muchachos`/`Muchachos` 一首歌
  - `Messi FC` 阿萨塔

- YouTube quality 设置成 144p 会影响音质

- .py文件默认Python IDLE打开 Open with → Choose another app →
  -  `C:\Program Files\Python312\Lib\idlelib\idle.bat`
  -  `C:\Users\YOUR_USER_NAME\AppData\Local\Programs\Python\Python312\Lib\idlelib\idle.bat`

- `link.com` 无法做到`不自动Send Code`，所以我注销了。


- environment variables

  ```powershell
  [Environment]::SetEnvironmentVariable("MY_VAR", "hello", "User")
  [Environment]::SetEnvironmentVariable("MY_VAR", "hello", "Machine") # 系统范围（管理员）
  $env:MY_VAR = "hello"
  setx MY_VAR "hello"
  setx MY_VAR "hello" /M # 系统范围（管理员）
  notepad $PROFILE # 每次打开 PowerShell 都自动有这个变量，但不想污染系统环境变量，可以写到 profile
  ```
